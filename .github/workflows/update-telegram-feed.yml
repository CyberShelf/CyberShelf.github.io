name: Update Telegram feed cache

on:
  schedule:
    - cron: '17 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: telegram-feed-cache
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Fetch and validate feed
        run: node scripts/update-telegram-feed.mjs
      - name: Commit cache when changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/telegram-posts.json assets/images/telegram/
          git diff --cached --quiet || git commit -m "chore: به‌روزرسانی کش تلگرام"
          git push
